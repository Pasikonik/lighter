.text-center.video-title 
  = @video.title
.text-center
  = video_tag @video.source_url.to_s, width: 640, controls: true, autobuffer: true, class: "red-border"
%br
.col-md-2.col-md-offset-3
  wrzucił:
  = @video.user.email
.col-md-2.text-center
  wyświetleń:
  10
.col-md-2.text-center
  - if current_user
    - unless @video.average("score").nil?
      = @video.average("score").avg.round(1)
    = rating_for @video, "score", disable_after_rate: false
- if @video.description?
  %br
  %p.col-md-6.col-md-offset-3.video-description.well
    = @video.description
